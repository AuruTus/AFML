# Python Environment: pyenv + `mlquant`

This project uses **pyenv** with the `mlquant` environment.

```bash
pyenv shell mlquant
```

- Always activate `mlquant` before running any Python code in this repo.
- The `.python-version` file at the repo root enforces this.
- Verify with `python --version` and `which python`.
